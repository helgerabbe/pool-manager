{
  "name": "SchuelerLernzielEinschaetzung",
  "type": "object",
  "description": "Selbsteinsch\u00e4tzung eines Sch\u00fclers zu einem Lernziel auf der Lernlandkarte (sys_map_full). Pro (user_email, einheit_id, lernziel_id) maximal ein Datensatz (anwendungsseitig sichergestellt). Vier Stufen von rot nach gr\u00fcn: 'schwierig' = Kein Plan, 'unsicher' = Ein bisschen verstanden, 'teilweise' = Kann das meiste, 'sicher' = Kann ich. Kein Eintrag = noch nicht eingesch\u00e4tzt. Reihenfolge und Beschriftung liegen zentral in src/lib/lernlandkarteEinschaetzung.js.",
  "properties": {
    "user_email": {
      "type": "string",
      "description": "E-Mail des Sch\u00fclers."
    },
    "einheit_id": {
      "type": "string",
      "description": "FK zur Einheit (f\u00fcr schnelle Abfrage aller Einsch\u00e4tzungen einer Einheit)."
    },
    "lernziel_id": {
      "type": "string",
      "description": "FK zum Lernziel (Lernziele-Entity)."
    },
    "lernpaket_id": {
      "type": "string",
      "description": "Denormalisiert: Lernpaket, zu dem das Lernziel geh\u00f6rt (f\u00fcr Auswertungen)."
    },
    "einschaetzung": {
      "type": "string",
      "enum": [
        "sicher",
        "teilweise",
        "unsicher",
        "schwierig"
      ],
      "description": "Selbsteinsch\u00e4tzung des Sch\u00fclers zu diesem Lernziel (4 Stufen, siehe Beschreibung der Entity). 'teilweise' wurde 2026-09-05 mit der vierstufigen Skala der Lernlandkarte erg\u00e4nzt."
    }
  },
  "required": [
    "user_email",
    "einheit_id",
    "lernziel_id",
    "einschaetzung"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "data.user_email": "{{user.email}}"
    },
    "update": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}